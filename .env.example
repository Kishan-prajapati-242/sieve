# Copy to .env and adjust. .env is gitignored; keep this file current.

POSTGRES_USER=sieve
POSTGRES_PASSWORD=sieve
POSTGRES_DB=sieve

# Used from the host: pytest, bench/ scripts, psql.
# Inside docker compose, the api service overrides the host with "postgres".
DATABASE_URL=postgresql://sieve:sieve@localhost:5432/sieve
